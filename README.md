# My Portfolio

A simple profile page built with React, Vite, and Tailwind CSS. It shows my name,
my goal for the course, and a `StatusBadge` that switches between "Open to work"
and "Busy learning" based on a prop.

## Setup

```bash
pnpm install
pnpm dev
```
